# ==================================================
# Fermi API Gateway - Production Environment Config
# ==================================================
#
# IMPORTANT: Update all values marked with TODO before deploying!
#

# Server Configuration
PORT=8080
ENV=production

# CORS Configuration
# TODO: Update with your actual frontend domains
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com,https://www.yourdomain.com

# Backend URLs
# TODO: Update with your actual backend service URLs

# Rollup Service URL
ROLLUP_URL=http://localhost:3000

# Continuum gRPC Service (format: host:port, no protocol)
CONTINUUM_GRPC_URL=localhost:9090

# Continuum REST Service URL (include protocol and full base path)
CONTINUUM_REST_URL=http://localhost:8081/api/v1

# Rate Limiting Configuration (requests per minute per IP)
# Adjust these values based on your expected traffic and capacity

# Rollup API rate limit (default: 1000 req/min)
RATE_LIMIT_ROLLUP=1000

# Continuum gRPC API rate limit (default: 500 req/min)
RATE_LIMIT_CONTINUUM_GRPC=500

# Continuum REST API rate limit (default: 2000 req/min)
RATE_LIMIT_CONTINUUM_REST=2000

# ==================================================
# Production Checklist:
# ==================================================
#
# [ ] Updated ALLOWED_ORIGINS with actual frontend domains
# [ ] Updated ROLLUP_URL with actual service URL
# [ ] Updated CONTINUUM_GRPC_URL with actual service URL
# [ ] Updated CONTINUUM_REST_URL with actual service URL
# [ ] Adjusted rate limits based on capacity
# [ ] Verified backend services are accessible from this server
# [ ] Configured firewall rules (ports 80, 443, 8080)
# [ ] SSL certificate obtained and configured
# [ ] Nginx reverse proxy configured
# [ ] Systemd service enabled and running
# [ ] Monitoring and alerting configured
# [ ] Log rotation configured
# [ ] Backup strategy in place
#
